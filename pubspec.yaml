name: ridesafe_api
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.6 <3.0.0'

# dependencies:
#   path: ^1.8.0

dependencies:
  ridesafe_core:
    git: https://github.com/splitscale/ridesafe_core.git

  ridesafe_cloud_database_impl:
    git: https://github.com/splitscale/ridesafe_cloud_database_impl.git

  ridesafe_local_database_impl:
    git: https://github.com/splitscale/ridesafe_local_database_impl.git

  ridesafe_bluetooth_impl:
    git: https://github.com/splitscale/ridesafe_bluetooth_impl.git

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
